[project]
name = "wapsa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "stnrliza", email = "contactsitinurhaliza@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]

[project.scripts]
wapsa = "wapsa:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
